apiVersion: v1
kind: Service
metadata:
  name: movie-bot-service
  labels:
    app: movie-bot
spec:
  type: NodePort
  selector:
    app: movie-bot
  ports:
    - name: http
      port: 80
      targetPort: 8110
      nodePort: 30110